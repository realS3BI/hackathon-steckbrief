# Optional: without a password, anyone with the URL can read and create profiles.
TEAM_PASSWORD=
# Local development only. Compose uses /app/data/huettentoene.sqlite.
DATABASE_PATH=./data/huettentoene.sqlite
# Optional host port for compose.local.yaml.
APP_PORT=3000
